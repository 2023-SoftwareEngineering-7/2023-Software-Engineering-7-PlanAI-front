/*package com.example.planai_front.hbserver;

import com.example.planai_front.WriteActivity;

import retrofit2.Call;
import retrofit2.http.Body;
import retrofit2.http.POST;


public interface ApiSer {
    //TODO:서버에서 정의된 엔드포인트=> 유저 등록 먼저 하고, 해당 유저 id를 불러올것
    @POST("user/1/Schedule")
    // 여기서 "schedule/create"는 서버에서 정의된 엔드포인트를 나타냅니다.
    Call<Server_ScheduleDTO> createSchedule(@Body Server_ScheduleDTO schedule);

    @POST("user/1/task")
    Call<Server_TaskDTO> createTask(@Body Server_TaskDTO task);
}*/

