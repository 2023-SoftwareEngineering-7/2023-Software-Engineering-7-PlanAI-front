<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:dist="http://schemas.android.com/apk/distribution"
        xmlns:tools="http://schemas.android.com/tools"
        tools:targetApi="31">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" /> <!-- 서버 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:name=".create.MainCalendarApplication"
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:networkSecurityConfig="@xml/network_security_config"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.PlanAI_front">
        <activity
                android:name=".Profile.ChangeAccount"
                android:exported="false" />
        <activity android:name=".WriteActivity" />
        <activity android:name=".MywriteActivity" />
        <activity android:name=".GetfriendActivity" />
        <activity android:name=".FriendlistActivity" />
        <activity android:name=".AddfriendActivity" />
        <activity android:name=".Profile.ChangeInformationActivity" />
        <activity android:name=".BoardActivity" />
        <activity android:name=".Profile.SettingActivity" />
        <activity
                android:name=".MainActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GoogleSignInActivity" />
        <activity android:name=".MaterialCalendarActivity" />
        <activity android:name=".create.ShowDayActivity" />
        <activity
                android:name=".create.CreateScheduleActivity"
                android:theme="@style/PopupActivity"></activity>
        <activity
                android:name=".create.CreateTaskActivity"
                android:theme="@style/PopupActivity"></activity>
    </application>

</manifest>